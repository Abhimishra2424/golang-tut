package main

import (
	"fmt"
	"strings"
)

func main() {

	// shopingCard := make(map[strings]int)

	shopingCard := map[string]int{
		"keyboard": 100,
		"mouse":    50,
	}
	fmt.Println(shopingCard)
}
